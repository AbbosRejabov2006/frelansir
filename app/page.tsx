"use client"

// Re-use the application that already lives in /src
import App from "../src/App"

// Import the extra CSS (light-theme helpers) that lives in /src
import "../src/index.css"

export default function Page() {
  return <App />
}
